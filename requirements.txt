# 养老金融通报数据自动化处理系统
# 依赖包列表

# 核心依赖
openpyxl>=3.1.0
pandas>=2.0.0
numpy>=1.24.0

# GUI依赖 (Python内置tkinter,无需额外安装)
# 注意: tkinter在Windows和macOS上通常预装
# Linux: sudo apt-get install python3-tk

# 打包依赖
pyinstaller>=6.0.0
